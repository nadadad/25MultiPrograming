
int main() 
{
    int i = 0;

    while (i < 9) // 9번 반복
    {
        puts("오징어");
        i = i + 1;

        if (i % 3 == 0) // i가 3의 배수일 때
        {
            puts("*맛있다");
        }
    }

    return 0;
}
